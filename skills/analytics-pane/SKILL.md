---
name: analytics-pane
description: "Add Analytics Pane support with dynamic reference lines."
---

# Power BI Visual — Analytics Pane

The Analytics Pane allows a Power BI custom visual to present dynamic reference lines (min, max, average, trend lines) that help users identify important trends or insights. These lines overlay the visual and highlight important data patterns. The Analytics Pane is separate from the Format Pane — it should **only** contain objects that add new informational content (e.g., reference lines), not visual styling options.

## Trigger

**Use when:**

- The user asks to implement power bi visual — analytics pane in a Power BI custom visual.
- The user needs guidance on the analytics-pane feature or API.

**Don't use when:**

- The user is working with a standard (non-custom) Power BI visual that does not support the visuals SDK.

## Overview

The Analytics Pane allows a Power BI custom visual to present dynamic reference lines (min, max, average, trend lines) that help users identify important trends or insights. These lines overlay the visual and highlight important data patterns. The Analytics Pane is separate from the Format Pane — it should **only** contain objects that add new informational content (e.g., reference lines), not visual styling options.

See [references/README.md](references/README.md) for a detailed walkthrough, code samples, and a checklist.
