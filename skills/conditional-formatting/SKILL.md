---
name: conditional-formatting
description: "Add conditional formatting to dynamically change colors based on data values."
---

# Power BI Visual — Add Conditional Formatting

Conditional formatting lets report creators dynamically change colors based on numerical values, rules, or gradients. When enabled, an **fx button** appears next to color properties in the Format pane. Clicking the fx button opens a dialog where users can define rules (e.g., "if value > 100, color = red") or bind colors to a data field gradient.

## Trigger

**Use when:**

- The user asks to implement power bi visual — add conditional formatting in a Power BI custom visual.
- The user needs guidance on the conditional-formatting feature or API.

**Don't use when:**

- The user is working with a standard (non-custom) Power BI visual that does not support the visuals SDK.

## Overview

Conditional formatting lets report creators dynamically change colors based on numerical values, rules, or gradients. When enabled, an **fx button** appears next to color properties in the Format pane. Clicking the fx button opens a dialog where users can define rules (e.g., "if value > 100, color = red") or bind colors to a data field gradient.

See [references/README.md](references/README.md) for a detailed walkthrough, code samples, and a checklist.
