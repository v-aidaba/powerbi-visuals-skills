---
name: drill-down
description: "Add drill-down support to navigate through data hierarchies."
---

# Power BI Visual — Add Drill-Down Support

Drill-down support enables users to navigate through data hierarchies in a Power BI custom visual. Users can drill down from a high-level view (e.g., Year) to more detailed levels (e.g., Quarter → Month → Day). This is configured through the `drilldown` property in `capabilities.json`.

## Trigger

**Use when:**

- The user asks to implement power bi visual — add drill-down support in a Power BI custom visual.
- The user needs guidance on the drill-down feature or API.

**Don't use when:**

- The user is working with a standard (non-custom) Power BI visual that does not support the visuals SDK.

## Overview

Drill-down support enables users to navigate through data hierarchies in a Power BI custom visual. Users can drill down from a high-level view (e.g., Year) to more detailed levels (e.g., Quarter → Month → Day). This is configured through the `drilldown` property in `capabilities.json`.

See [references/README.md](references/README.md) for a detailed walkthrough, code samples, and a checklist.
