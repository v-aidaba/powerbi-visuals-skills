---
name: file-download-api
description: "Download data from a custom visual into a file on the user's device."
---

# Power BI Visual — File Download API

The File Download API lets users download data from a custom visual into a file on their device. The API supports multiple file formats (.txt, .csv, .json, .xml, .pdf, .xlsx, .tmplt) and requires both a privilege declaration in capabilities.json and admin permission.

## Trigger

**Use when:**

- The user asks to implement power bi visual — file download api in a Power BI custom visual.
- The user needs guidance on the file-download-api feature or API.

**Don't use when:**

- The user is working with a standard (non-custom) Power BI visual that does not support the visuals SDK.

## Overview

The File Download API lets users download data from a custom visual into a file on their device. The API supports multiple file formats (.txt, .csv, .json, .xml, .pdf, .xlsx, .tmplt) and requires both a privilege declaration in capabilities.json and admin permission.

See [references/README.md](references/README.md) for a detailed walkthrough, code samples, and a checklist.
