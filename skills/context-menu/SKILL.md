---
name: context-menu
description: "Add right-click context menu support to a Power BI custom visual."
---

# Power BI Visual — Add Context Menu Support

Every Power BI visual can display a right-click context menu that enables operations like analyze, summarize, copy, drill-through, and more. There are two modes: clicking on empty space shows a basic context menu, while clicking on a data point shows additional options (Show data as table, Include, Exclude filters).

## Trigger

**Use when:**

- The user asks to implement power bi visual — add context menu support in a Power BI custom visual.
- The user needs guidance on the context-menu feature or API.

**Don't use when:**

- The user is working with a standard (non-custom) Power BI visual that does not support the visuals SDK.

## Overview

Every Power BI visual can display a right-click context menu that enables operations like analyze, summarize, copy, drill-through, and more. There are two modes: clicking on empty space shows a basic context menu, while clicking on a data point shows additional options (Show data as table, Include, Exclude filters).

See [references/README.md](references/README.md) for a detailed walkthrough, code samples, and a checklist.
